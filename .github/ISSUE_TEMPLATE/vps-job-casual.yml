name: VPS Job Casual
description: Send a short Japanese request to the VPS
title: "[VPS Job] ai-councilの状態見て"
labels:
  - vps-job
body:
  - type: markdown
    attributes:
      value: |
        スマホから短い日本語でVPSへ安全な確認jobを送るフォームです。

        今は決まった言い方だけを、許可済みjobへ変換します。
        自由に書きたい場合は `VPS Free Request` を使ってください。
        実作業をVPS上のAI CLIへ依頼する場合は `VPS AI Exec` を使ってください。

  - type: textarea
    id: request
    attributes:
      label: やってほしいこと
      description: 普通の確認ならこのまま投稿できます。
      value: ai-councilの状態見て
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        使える言い方:

        - `ai-councilの状態見て`
        - `ai-councilをチェックして`
        - `ai-councilを検証して`
        - `ai-councilをチェックだけして`
        - `作業場まとめて`
        - `ワークスペース一覧見て`

  - type: checkboxes
    id: safety
    attributes:
      label: Safety check
      options:
        - label: パスワード、token、SSH秘密鍵、APIキー、その他secretsを書いていません。
          required: true
        - label: 任意のshell command実行は依頼していません。
          required: true
