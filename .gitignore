# OS / editor noise
.DS_Store
*.swp
*~
.idea/
.vscode/

# Local state and logs (must never be committed)
*.log
*.local
*.tmp

# Secrets and credentials — keep all of these out of the repository
.env
*.env
*.key
*.pem
secrets/
**/allowlist.txt
**/*credentials*
